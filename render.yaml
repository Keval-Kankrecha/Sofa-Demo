services:
  - type: web
    name: sofa-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # or whatever your entry point is
    environment:
      pythonVersion: 3.10
